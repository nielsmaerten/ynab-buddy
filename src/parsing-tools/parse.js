module.exports = (csvString, filename, config) => {
  // TODO: Implement after test-config.js
  // Parses the CSV string using the provided config,
  // then returns a result object
  return {
    success: true,
    name: filename,
    transactions: [
      /** TODO */
    ]
  };
};
